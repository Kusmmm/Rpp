import React, { Component, PropTypes } from 'react';

export default class LoginScreen extends Comment{
    render(){
        return(
            <div>
                Hi!
            </div>
        )
    }
}